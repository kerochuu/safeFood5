<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">


	<select id="searchId" resultType="User" parameterType="String">
		select user_id, user_pw, user_name, user_phone, user_address, user_eatlist, user_allergy
	
		from user
		where user_id=#{user_id}
	</select>
	
	<select id="searchCode" resultType="User" parameterType="int">
		select user_code
		from user
		where user_code=#{user_code}
	</select>


	<select id="findUserWithFoodList" resultType="User" parameterType = "int">
		select user_id, user_name, user_eatlist
		from user
		where user_code = #{user_code}
	</select>


	<insert id="insert" parameterType="User">
		insert into user( user_id, user_pw, user_name, user_phone, user_address, user_eatlist, user_allergy) 
		values( #{user_id}, #{user_pw}, #{user_name}, #{user_phone}, #{user_address}, #{user_eatlist}, #{user_allergy})
	</insert>
	
	<update id="changePw" parameterType="User">
		update 	user
		set 	user_pw = "0000"
		where 	user_id = #{user_id}
	</update>

	<update id="changeInfo" parameterType="User">
		update 	user
		set 	user_address = #{user_address},
				user_phone = #{user_phone},
				user_pw = #{user_pw}
		where 	user_id = #{user_id}
	</update>
	
	<delete id="deleteInfo" parameterType="User">
		delete  from user 
		where   user_id = #{user_id}
	</delete>
	
</mapper>















